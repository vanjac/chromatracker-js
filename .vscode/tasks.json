{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build index.html",
            "type": "shell",
            "command": "node build.mjs",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true,
            },
        },
    ]
}