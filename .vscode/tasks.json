{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build index.html",
            "type": "shell",
            "command": "npx liquidjs --jekyll-include --template '@Main.html' --output ../build.html",
            "options": {
                "cwd": "${workspaceFolder}/_includes"
            },
            "group": {
                "kind": "build",
                "isDefault": true,
            },
        },
    ]
}