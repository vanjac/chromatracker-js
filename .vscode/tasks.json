{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build index.html",
            "type": "shell",
            "command": "npx liquidjs -t '@Main.html' -o ../build.html --jekyll-include --no-dynamic-partials",
            "options": {
                "cwd": "${workspaceFolder}/_includes"
            },
            "group": {
                "kind": "build",
                "isDefault": true,
            },
        },
    ]
}